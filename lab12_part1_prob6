#include <iostream>
#include <fstream>
using namespace std;
int main() {
    ifstream read("source.txt");
    ofstream write("copy.txt");
    string line;
    while (getline(read, line)) {
        write << line << endl;
    }
    read.close();
    write.close();
}
