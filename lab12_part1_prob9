#include <iostream>
#include <fstream>
using namespace std;
int main() {
    ifstream readline("text.txt");
    string line;
    int count = 0;
    while (getline(readline, line)) {
        count++;
    }
    cout <<"Lines read: "<< count << endl;
}
